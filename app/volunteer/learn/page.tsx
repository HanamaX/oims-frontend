"use client";

import React from "react";
import { redirect } from "next/navigation";

// This is a placeholder file to fix build issues
export default function VolunteerLearnPage() {
  // Redirect to the main volunteer page since this page is not implemented
  redirect("/volunteer");
}
